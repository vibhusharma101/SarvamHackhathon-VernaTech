# QA Audit (report-only) — 2026-07-26

_Scope: scaffold integrity, not feature QA — no live SARVAM_API_KEY or Supabase project exists yet, so the
voice pipeline, real scoring, and harness cannot be exercised end-to-end. This pass confirms the skeleton
boots, routes render, and error paths degrade gracefully rather than crashing._

**Limitation:** Playwright MCP (the tool `/vii-browse` normally drives) was not available in this
environment, so real-browser interaction (clicks, mic permission prompts, client-side console errors) could
not be captured. Verified instead via: `next build`, `tsc --noEmit`, `eslint`, direct HTTP fetch of
server-rendered HTML for all four routes, and direct API calls against a running (uncredentialed) backend.
This is a materially weaker check than a real browser pass — flagging so it isn't mistaken for one.

## Test suite
- No test suite exists in either `/api` or `/web` (none was in scope for this scaffold — see `.vii/plan.md`).
- Build/lint/typecheck (the closest equivalent): `next build` 0 errors, `tsc --noEmit` 0 errors, `eslint .` 0
  issues, all pass.

## Checks performed

1. Booted `uvicorn main:app` with no `.env` configured → `/health` returns 200; DB-backed routes
   (`GET /sessions`, `GET /harness/results`) correctly return 500 rather than hanging or silently returning
   empty data (matches `db.py`'s explicit `RuntimeError` when Supabase creds are missing).
2. Booted `next dev` pointed at that API → all four routes (`/`, `/console`, `/harness`,
   `/screen/test-session-1`) return HTTP 200 and their server-rendered HTML contains the expected static
   copy ("Recruiter console", "Select a session to view its scorecard", "Fairness harness", "Pick the
   language"). No Next.js error-boundary markers (`application error`, `__next_error__`, `digest`) present
   in any response.
3. `POST /admin/reset` with no `X-Admin-Token` header → 403. With a wrong token → 403. Confirms the
   post-security-review fix fails closed as intended.

## Bugs found

### BLOCKER
None found within the scope this pass could actually check.

### MAJOR
None found within the scope this pass could actually check.

### MINOR
- [ ] Client-side fetch failures (e.g. `GET /sessions` 500) are swallowed with `.catch(() => {})` in
      `web/app/console/page.tsx` and `web/app/harness/page.tsx` — the user sees a silent empty list
      ("No sessions yet") with no distinction between "genuinely empty" and "the API is down/misconfigured."
      Not verifiable from outside a real browser whether this also logs to the console (it does call
      `.catch`, so no unhandled-rejection warning, but there's also no visible error state).
      Suggested fix: surface a small inline "couldn't reach the API" banner when the fetch throws, distinct
      from the empty-state copy.
- [ ] Not independently verified in this pass (no browser): mic permission handling in
      `MicCapture.tsx`/`lib/audio.ts` when `getUserMedia` is denied — the code path exists (catches the
      rejection and shows an "unavailable, use file upload instead" message) but was only read-reviewed, not
      exercised against a real permission-denial in a browser.
      Suggested fix: none needed unless a future browser-driven QA pass finds it doesn't behave as read.

## Summary
0 blockers, 0 majors, 2 minors — both about the depth of what could be verified without a real browser, not
confirmed defects. Re-run `/vii-qa` (or `/vii-qa-only`) with Playwright MCP available, and with real Sarvam +
Supabase credentials configured, before treating the live voice/scoring/harness paths as verified — this pass
only confirms the scaffold's shell doesn't crash.
